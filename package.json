{
    "private": true,
    "name": "seabasket-backend-api",
    "description": "SeaBasket Laravel Backend API - serves both web and mobile frontends",
    "scripts": {
        "dev": "echo 'Laravel backend API - no frontend build required'",
        "build": "echo 'Laravel backend API - no frontend build required'"
    },
    "devDependencies": {
        "axios": "^1.8.2",
        "concurrently": "^9.0.1"
    },
    "dependencies": {}
}
